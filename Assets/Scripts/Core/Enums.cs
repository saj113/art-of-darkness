namespace Core
{
    public enum Direction { None, Back, Forward, Round }

    public enum ModificatorExtensionNumber { Default, FirstExtension, SecondExtension }
    public enum ExperienceAmoutType { Quest, Boss, SubLayer }
    public enum CompanionName { Default }
    public enum BuffUniqueCode { NoUnique, AbsorbShieldUnique }
}
 